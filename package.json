{
  "name": "kindle-highlight-to-scrapbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:esbuild && npm run build:bookmarklet",
    "build:esbuild": "esbuild ./src/index.ts --bundle --outfile=dist/index.js --minify",
    "build:bookmarklet": "node ./build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.17.15"
  },
  "dependencies": {
    "kindle-highlight-to-markdown": "^1.3.1"
  }
}
